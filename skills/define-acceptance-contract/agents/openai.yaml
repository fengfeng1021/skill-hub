interface:
  display_name: "需求契約"
  short_description: "把模糊需求整理成可追蹤、可驗證且不會被偷偷降低的完成條件"
  default_prompt: "Use $define-acceptance-contract to turn this request into a testable acceptance contract before implementation."

policy:
  allow_implicit_invocation: true
