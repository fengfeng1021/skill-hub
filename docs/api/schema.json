{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://skill-hub/registry/schema.json",
  "title": "Skill Hub Registry Entry",
  "description": "registry/skills/<id>.json 的資料格式。每個檔案代表一個被收錄的 skill。",
  "type": "object",
  "required": ["id", "name", "summary", "category", "tags", "source", "install"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "唯一識別碼，kebab-case。同時作為檔名與網址錨點。"
    },
    "name": {
      "type": "string",
      "description": "顯示名稱。"
    },
    "summary": {
      "type": "string",
      "maxLength": 60,
      "description": "白話一句話，卡片上顯示的就是這句。寫作規則：(1) 繁體中文，25 字內；(2) 只講「能幫我做到什麼」，不講「用什麼技術做到」；(3) 不要出現 API、函式庫、外掛、框架、frontmatter、tween 這類術語，也不要照抄英文 description；(4) 標準是「完全不懂程式的人看一眼就知道這是幹嘛的」。好例子：「讓網頁上的東西動起來，淡入、滑動、跟著捲動變化都能做。」壞例子：「GreenSock 官方動畫 skill 全套：tween、時間軸、ScrollTrigger、外掛與效能。」技術細節寫進 description，不要塞在這裡。"
    },
    "description": {
      "type": "string",
      "description": "完整說明，詳情面板顯示。這裡才寫技術細節、涵蓋範圍與專有名詞。可多段（用 \\n 分隔）。"
    },
    "category": {
      "type": "string",
      "description": "分類名稱，直接寫顯示文字，例如「前端 / 動效」。"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "標籤，用於篩選與搜尋。"
    },
    "author": { "type": "string", "description": "作者 / 組織。" },
    "license": { "type": "string", "description": "授權，例如 MIT。" },
    "version": { "type": "string", "description": "版本字串。" },
    "official": {
      "type": "boolean",
      "description": "是否為原作者或原廠官方出品。"
    },
    "featured": {
      "type": "boolean",
      "description": "是否置頂推薦。"
    },
    "source": {
      "type": "object",
      "required": ["kind"],
      "additionalProperties": false,
      "description": "skill 的來源。決定安裝提示詞怎麼寫。",
      "properties": {
        "kind": {
          "enum": ["github", "local", "url"],
          "description": "github = 收錄外部 repo；local = 檔案存在本 repo 的 skills/ 底下；url = 單一檔案或壓縮檔網址。"
        },
        "url": { "type": "string", "description": "repo 或檔案的完整網址。" },
        "subdir": {
          "type": "string",
          "description": "kind=github 時，skill 位於 repo 內的相對路徑，例如 skills/gsap-core。留空代表 repo 根目錄就是 skill。"
        },
        "branch": { "type": "string", "description": "預設 main。" },
        "path": {
          "type": "string",
          "description": "kind=local 時，相對於本 repo 的路徑，例如 skills/my-skill。"
        }
      }
    },
    "parts": {
      "type": "array",
      "description": "同一個來源套件拆成多個要一起安裝的 skill 資料夾時使用，例如 GSAP 的 core / timeline / scrolltrigger。這些資料夾仍屬同一筆收錄；跨來源精選組合包請用 includes。",
      "items": {
        "type": "object",
        "required": ["dirName"],
        "additionalProperties": false,
        "properties": {
          "dirName": {
            "type": "string",
            "description": "安裝後的資料夾名，必須等於該 SKILL.md frontmatter 的 name。"
          },
          "name": { "type": "string", "description": "顯示名稱，例如「ScrollTrigger」。省略時用 dirName。" },
          "summary": { "type": "string", "description": "這一份負責什麼，一句話。同樣要白話，規則見上面的 summary。" },
          "subdir": {
            "type": "string",
            "description": "相對於 repo 根目錄的路徑。省略時自動組成 <source.subdir>/<dirName>。"
          }
        }
      }
    },
    "includes": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "description": "跨來源精選組合包引用的其他 registry id。建置時會遞迴展開、去重，安裝提示詞會把入口 skill 與所有引用項目一起安裝。這表示產品層級的組合，不是額外相依，也不是 parts。"
    },
    "replaces": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$"
      },
      "description": "此 skill 取代的舊資料夾／frontmatter name。安裝提示詞會要求偵測、比較並在新入口驗證後安全停用無自訂的舊入口，避免重複觸發。"
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "description": "可選的執行期工作流 metadata。includes 回答『要安裝什麼』；runtime 回答『入口執行時如何分階段、按需載入與續跑』。實際細節仍以入口 SKILL.md 為準。",
      "required": ["mode"],
      "properties": {
        "mode": {
          "enum": ["pipeline", "router", "overlay", "standalone"],
          "description": "pipeline = 有序階段；router = 依條件選路；overlay = 全程覆蓋；standalone = 可獨立執行。"
        },
        "stateArtifact": {
          "type": "string",
          "description": "跨回合保存狀態的專案相對路徑，例如 .agent/frontend-workflow.md。"
        },
        "portableCore": {
          "type": "boolean",
          "description": "入口是否只靠開放 Agent Skills 格式與相對資源就能完成基線工作流；平台專屬能力只能作增強。"
        },
        "controller": {
          "type": "object",
          "additionalProperties": false,
          "required": ["script", "stateSchema"],
          "properties": {
            "script": {
              "type": "string",
              "description": "相對於入口 skill 根目錄的可執行狀態控制器。"
            },
            "stateSchema": {
              "type": "string",
              "description": "相對於入口 skill 根目錄的狀態 JSON Schema。"
            }
          }
        },
        "continueWithoutUserInput": {
          "type": "boolean",
          "description": "通過 Gate 後是否自動進入下一階段。"
        },
        "ambiguityPolicy": {
          "enum": ["ask-only-material", "ask-before-change", "autonomous"],
          "description": "什麼情況需要停下來向使用者確認。"
        },
        "stages": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "name", "skills", "gate"],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9-]*$"
              },
              "name": { "type": "string" },
              "skills": {
                "type": "array",
                "uniqueItems": true,
                "items": {
                  "type": "string",
                  "pattern": "^[a-z0-9][a-z0-9-]*$"
                }
              },
              "optionalSkills": {
                "type": "array",
                "uniqueItems": true,
                "items": {
                  "type": "string",
                  "pattern": "^[a-z0-9][a-z0-9-]*$"
                }
              },
              "coreReferences": {
                "type": "array",
                "uniqueItems": true,
                "items": { "type": "string" },
                "description": "入口 skill 內建、在沒有外部子 skill 時仍能執行此階段的相對 reference。"
              },
              "when": { "type": "string" },
              "outputs": {
                "type": "array",
                "items": { "type": "string" }
              },
              "gate": { "type": "string" }
            }
          }
        },
        "overlays": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          }
        },
        "finalizers": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          }
        }
      }
    },
    "install": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "dirName": {
          "type": "string",
          "description": "安裝後的資料夾名稱，通常等於 SKILL.md frontmatter 的 name。有 parts 時可省略。"
        },
        "command": {
          "type": "string",
          "description": "來源提供的一行安裝指令。只有不綁特定 Agent 或固定 skills 路徑時，提示詞才會請 AI 優先使用。"
        },
        "scope": {
          "enum": ["user", "project"],
          "description": "使用範圍偏好：user = 目前 Agent 支援的全域範圍；project = 目前 Agent 支援的專案範圍。這不是固定路徑，安裝時必須依當前 Agent 的官方機制判定。預設 user。"
        },
        "files": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string" },
          "description": "此 skill 包含的檔案清單（相對於 skill 資料夾），例如 [\"SKILL.md\", \"references/api.md\"]。"
        },
        "directories": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string" },
          "description": "必須從來源遞迴複製且保留結構的完整子目錄，例如 [\"references\", \"scripts\"]。上游日後在目錄內新增檔案也屬安裝內容；不得把檔案攤平到 skill 根目錄。"
        },
        "requires": {
          "type": "array",
          "items": { "type": "string" },
          "description": "額外相依，例如 [\"node>=18\", \"pip install pypdf\"]。安裝提示詞會提醒 AI 處理。"
        },
        "notes": {
          "type": "string",
          "description": "安裝的特殊注意事項，會原樣插進提示詞。"
        }
      }
    },
    "usage": {
      "type": "array",
      "items": { "type": "string" },
      "description": "什麼情況會觸發／該用這個 skill。"
    },
    "highlights": {
      "type": "array",
      "items": { "type": "string" },
      "description": "重點功能，條列。"
    },
    "prompt": {
      "type": ["string", "null"],
      "description": "自訂的 AI 安裝提示詞。留 null 代表由 build 依照 source/install 自動產生。"
    },
    "links": {
      "type": "array",
      "description": "相關連結。",
      "items": {
        "type": "object",
        "required": ["label", "url"],
        "additionalProperties": false,
        "properties": {
          "label": { "type": "string" },
          "url": { "type": "string" }
        }
      }
    },
    "addedAt": { "type": "string", "description": "收錄日期 YYYY-MM-DD。" },
    "updatedAt": { "type": "string", "description": "最後更新日期 YYYY-MM-DD。" }
  }
}
